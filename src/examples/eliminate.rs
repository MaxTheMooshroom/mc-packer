

pub fn binaryElimination(Vec<bool> arr) {
    let mut left:  u32 = 0;
    let mut right: u32 = arr.len() - 1;
    let mut mid:   u32;

    while (left <= right) {
        mid = (left + right) / 2.0;
    }
}